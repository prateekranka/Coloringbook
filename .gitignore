# Xcode generated artefacts — regenerated by `xcodegen generate`
*.xcodeproj/
*.xcworkspace/
xcuserdata/

# Xcode build products
build/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# SwiftPM local cache (never belongs in the app target)
.build/

# Local agent / editor state
.claire/
.claude/

# Template pipeline working directory (rasters, intermediate SVGs, etc.)
pipeline_work/

# macOS
.DS_Store
