# Xcode generated artefacts — regenerated by `xcodegen generate`
*.xcodeproj/
*.xcworkspace/
xcuserdata/

# Xcode build products
build/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# SwiftPM local cache (never belongs in the app target)
.build/

# Local agent / editor state
.claire/
.claude/
.agent/

# Template pipeline working directory (rasters, intermediate SVGs, etc.)
pipeline_work/

# macOS
.DS_Store
.worktrees

# UI test screenshots and baselines (local only)
Scripts/testing/baselines/
Scripts/testing/.screenshots/

# Local diagnostic captures
artifacts/canvas-diagnostics/
artifacts/pencil-hid/

# Release cleanup: local/private/generated preservation candidates
Scripts/coredevice_tailnet_bridge.sh
docs/app-store/screenshots/generated/
gouache-smooth-deepseekv4pro/

# Local-only internal dev tooling
Tools/library-ui-tuner/

.lazyweb
.opencode/skills
